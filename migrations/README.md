# Migrations (histórico)

O schema completo está unificado em `database.sql` na raiz do projeto.

**Para nova instalação:**
```bash
mysql -u root -p < database.sql
```

Os arquivos `.sql` nesta pasta são referência histórica das alterações incrementais.
