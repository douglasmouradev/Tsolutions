-- Adiciona campos do Excel ao Cadastro de Técnicos
USE crm_chamados;

ALTER TABLE tecnicos
    ADD COLUMN naturalidade VARCHAR(100) NULL AFTER name,
    ADD COLUMN rg VARCHAR(30) NULL AFTER email,
    ADD COLUMN cpf VARCHAR(20) NULL AFTER rg,
    ADD COLUMN data_nascimento DATE NULL AFTER cpf,
    ADD COLUMN genero VARCHAR(20) NULL AFTER data_nascimento,
    ADD COLUMN nome_mae VARCHAR(120) NULL AFTER genero,
    ADD COLUMN nome_pai VARCHAR(120) NULL AFTER nome_mae,
    ADD COLUMN cep VARCHAR(20) NULL AFTER nome_pai,
    ADD COLUMN endereco VARCHAR(255) NULL AFTER cep,
    ADD COLUMN numero VARCHAR(20) NULL AFTER endereco,
    ADD COLUMN bairro VARCHAR(100) NULL AFTER numero,
    ADD COLUMN cidade VARCHAR(100) NULL AFTER bairro,
    ADD COLUMN estado VARCHAR(5) NULL AFTER cidade,
    ADD COLUMN pais VARCHAR(50) NULL AFTER estado,
    ADD COLUMN celular_1 VARCHAR(30) NULL AFTER pais,
    ADD COLUMN celular_2 VARCHAR(30) NULL AFTER celular_1,
    ADD COLUMN whats VARCHAR(30) NULL AFTER celular_2,
    ADD COLUMN telefone_fixo VARCHAR(30) NULL AFTER whats,
    ADD COLUMN referencia_bancaria VARCHAR(255) NULL AFTER telefone_fixo,
    ADD COLUMN chave_pix VARCHAR(100) NULL AFTER referencia_bancaria,
    ADD COLUMN banco VARCHAR(100) NULL AFTER chave_pix,
    ADD COLUMN cod_banco VARCHAR(10) NULL AFTER banco,
    ADD COLUMN agencia VARCHAR(20) NULL AFTER cod_banco,
    ADD COLUMN conta VARCHAR(30) NULL AFTER agencia,
    ADD COLUMN tipo_conta VARCHAR(30) NULL AFTER conta,
    ADD COLUMN operacao VARCHAR(20) NULL AFTER tipo_conta,
    ADD COLUMN favorecido VARCHAR(120) NULL AFTER operacao,
    ADD COLUMN razao_social VARCHAR(150) NULL AFTER favorecido,
    ADD COLUMN nome_fantasia VARCHAR(150) NULL AFTER razao_social,
    ADD COLUMN cnpj VARCHAR(20) NULL AFTER nome_fantasia,
    ADD COLUMN inscricao_estadual VARCHAR(30) NULL AFTER cnpj,
    ADD COLUMN inscricao_municipal VARCHAR(30) NULL AFTER inscricao_estadual,
    ADD COLUMN empresa_cep VARCHAR(20) NULL AFTER inscricao_municipal,
    ADD COLUMN empresa_endereco VARCHAR(255) NULL AFTER empresa_cep,
    ADD COLUMN empresa_numero VARCHAR(20) NULL AFTER empresa_endereco,
    ADD COLUMN empresa_bairro VARCHAR(100) NULL AFTER empresa_numero,
    ADD COLUMN empresa_cidade VARCHAR(100) NULL AFTER empresa_bairro,
    ADD COLUMN empresa_estado VARCHAR(5) NULL AFTER empresa_cidade,
    ADD COLUMN empresa_pais VARCHAR(50) NULL AFTER empresa_estado,
    ADD COLUMN empresa_referencia_bancaria VARCHAR(255) NULL AFTER empresa_pais,
    ADD COLUMN empresa_chave_pix VARCHAR(100) NULL AFTER empresa_referencia_bancaria,
    ADD COLUMN empresa_banco VARCHAR(100) NULL AFTER empresa_chave_pix,
    ADD COLUMN empresa_cod_banco VARCHAR(10) NULL AFTER empresa_banco,
    ADD COLUMN empresa_agencia VARCHAR(20) NULL AFTER empresa_cod_banco,
    ADD COLUMN empresa_conta VARCHAR(30) NULL AFTER empresa_agencia,
    ADD COLUMN empresa_tipo_conta VARCHAR(30) NULL AFTER empresa_conta,
    ADD COLUMN empresa_operacao VARCHAR(20) NULL AFTER empresa_tipo_conta,
    ADD COLUMN empresa_favorecido VARCHAR(120) NULL AFTER empresa_operacao;
